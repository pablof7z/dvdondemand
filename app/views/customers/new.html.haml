.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "All Customers", customers_path
      %li.active= link_to "New", new_customer_path
  .content
    %h2.title #{ t("web-app-theme.new", :default => "New")} Customer
    .inner
      - form_for @customer, :url => customers_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

- content_for :sidebar, render(:partial => 'sidebar')

