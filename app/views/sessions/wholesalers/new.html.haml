#content
  %h1 Login to the Wholesale Account
  .box.sp
    - form_for resource_name, resource, :url => session_path(resource_name), :html => { :class => :form } do |f|
      %label
        %span
          E-mail
        = f.text_field :email
      
      %label
        %span
          Password
        = f.password_field :password

      - if devise_mapping.rememberable?
        %label
          = f.check_box :remember_me
          Remember me

      %p.btso
        %button.b1{:type => 'submit'}
          Login

- content_for :sidebar, render(:partial => "shared/devise_wholesaler_links")

