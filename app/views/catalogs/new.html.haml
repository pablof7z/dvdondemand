.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "&laquo; #{@publisher.first_name} #{@publisher.last_name}", publisher_path(@publisher)
      %li= link_to "Catalogs", publisher_catalogs_path(@publisher)
      %li.active= link_to t("web-app-theme.new", :default => "New"), new_publisher_catalog_path(@publisher)
  .content
    %h2.title New Catalog for #{@publisher.first_name} #{@publisher.last_name}
    .inner
      - form_for @catalog, :url => publisher_catalogs_path(@publisher), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

- content_for :sidebar, render(:partial => 'sidebar')
