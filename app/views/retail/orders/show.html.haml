- content_for :primary_nav do
  =render :partial => 'retail/shared/navbar'

#content.wide
  %h1 Order Receipt

  = render :partial => 'receipt'

  .box
    %button.b2{:onclick => "javascript:window.open('#{retail_customer_order_path(current_customer, @order, :printable => true)}','','location=no,menubar=no,status=no,toolbar=no,width=500')"} Printable Version
