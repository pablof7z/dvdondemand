.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "&laquo; #{@publisher.first_name} #{@publisher.last_name}", publish_publisher_path(@publisher)
      %li= link_to "Products", publish_publisher_products_path(@publisher)
      %li.active= link_to "Wholesale Prices", publish_publisher_product_wholesale_prices_path(@publisher,@product)
      %li= link_to "New", new_publish_publisher_product_wholesale_price_path(@publisher,@product)
  .content
    %h2.title All Wholesale Prices for #{@product.title}
    .inner
      %table.table
        %tr
          %th.first ID
          %th Minimum Quantity
          %th Discount Percentage
          %th Enabled?
          %th.last &nbsp;
        - @wholesale_prices.each do |wholesale_price|
          %tr{:class => cycle("odd", "even")}
            %td= wholesale_price.id
            %td= link_to wholesale_price.minimum_quantity, publish_publisher_product_wholesale_price_path(@publisher,@product,wholesale_price)
            %td= number_to_percentage(wholesale_price.discount_percentage, :precision => 1)
            %td= wholesale_price.enabled ? 'Yes' : 'No'
            %td.last
              = link_to "Show", publish_publisher_product_wholesale_price_path(@publisher,@product,wholesale_price)
              |
              = link_to "Edit", edit_publish_publisher_product_wholesale_price_path(@publisher,@product,wholesale_price)
              |
              = link_to "Delete", publish_publisher_product_wholesale_price_path(@publisher,@product,wholesale_price), :method => :delete, :confirm => "Are you sure?"
      .actions-bar.wat-cf
        .actions

- content_for :sidebar, render(:partial => 'publish/shared/sidebar')

