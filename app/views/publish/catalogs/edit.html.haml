- content_for :primary_nav do
  =render :partial => 'publish/shared/navbar'

#content
  %h1 Edit Catalog
  .box.sp
    - form_for @catalog, :url => publish_publisher_catalog_path(current_publisher,@catalog), :html => { :class => :form } do |f|
      = render :partial => "form", :locals => {:f => f}

