.group
  = f.error_messages

%label
  %span
    Description
  = f.text_field :description
  %span.descr (Required)

%label
  %span
    Amount
  = f.text_field :amount
  %span.descr (Required)

%label
  %span
    Percentage
  = f.check_box :percentage
  %span.descr Apply amout as percentage (of sold product's price)

%p.btso.b1
  %button.button{:type => 'submit'}
    Save
  %span
    or
    = link_to "Cancel", admin_fees_path, :class => "button"

