- content_for :primary_nav do
  =render :partial => 'admin/shared/navbar'

#content
  %h1 New Wholesaler Invoice Payment
  .box.sp
    - form_for @wholesaler_payment, :url => admin_wholesaler_invoice_wholesaler_payments_path(@wholesaler, @wholesaler_invoice), :html => { :class => :form } do |f|
      = render :partial => "form", :locals => {:f => f}

- content_for :sidebar, render(:partial => 'admin/shared/sidebar')

