.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "&laquo; #{@publisher.first_name} #{@publisher.last_name}", publisher_path(@publisher)
      %li= link_to "Products", publisher_products_path(@publisher)
      %li.active= link_to "New", new_publisher_product_path(@publisher)
  .content
    %h2.title New Product for #{@publisher.first_name} #{@publisher.last_name}
    .inner
      - form_for @product, :url => publisher_products_path(@publisher), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

- content_for :sidebar, render(:partial => 'sidebar')

