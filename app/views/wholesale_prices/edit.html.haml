.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "&laquo; #{@publisher.first_name} #{@publisher.last_name}", publisher_path(@publisher)
      %li= link_to "Products", publisher_products_path(@publisher)
      %li= link_to "Wholesale Prices", publisher_product_wholesale_prices_path(@publisher,@product)
      %li= link_to "New", new_publisher_product_wholesale_price_path(@publisher,@product)
      %li.active= link_to "Edit", edit_publisher_product_wholesale_price_path(@publisher,@product)
  .content
    %h2.title Edit Wholesale Price for #{@product.title}
    .inner
      - form_for @wholesale_price, :url => publisher_product_wholesale_price_path(@publisher,@product,@wholesale_price), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

- content_for :sidebar, render(:partial => 'sidebar')

